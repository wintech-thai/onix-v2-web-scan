# Environment Variables for Onix v2 Web Scan

# Redis Configuration (Production - required when REDIS_HOST is set)
REDIS_HOST=localhost
REDIS_PORT=6379

# Encryption Configuration (Required - fallback for local development)
# Use 32-character key for AES-256
ENCRYPTION_KEY=your-32-character-encryption-key
# Use 16-character IV for AES-256-CBC
ENCRYPTION_IV=your-16-char-iv

# Node Environment
NODE_ENV=development

# Next.js Configuration
# Note: Port 5000 is used by macOS Control Center (AirPlay)
# Using 5001 as default to avoid conflicts on macOS
PORT=5001
